#include "main.h"

/**
 * mul - Multiplication two integers
 * @a: The first number to multiply
 * @b: The second the number to multiply with first number
 * Return the result of multiplication between a param and b param
 */
int mul(int a, int b)
{
	return (a * b);
}
